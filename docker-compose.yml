services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: outgo
      POSTGRES_USER: outgo
      POSTGRES_PASSWORD: outgo_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
